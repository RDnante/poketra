package com.example.poketra.repository;

import com.example.poketra.model.Reste_poketra;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Reste_poketraRepository extends JpaRepository<Reste_poketra,Integer> {
}
