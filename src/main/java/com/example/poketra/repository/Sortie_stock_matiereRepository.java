package com.example.poketra.repository;

import com.example.poketra.model.Sortie_stock_matiere;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Sortie_stock_matiereRepository extends JpaRepository<Sortie_stock_matiere,Integer> {
}
